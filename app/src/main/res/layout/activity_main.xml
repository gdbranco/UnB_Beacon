<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="unb.beacon.beacon_project.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Beacon Project!"
        android:id="@+id/textView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/broadcast_uid"
        android:id="@+id/broadcast_btn"
        android:layout_below="@+id/textView"
        android:layout_alignParentEnd="true"
        android:enabled="true"
        android:allowUndo="false" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/locator_btn"
        android:id="@+id/locator_btn"
        android:layout_below="@+id/broadcast_btn"
        android:layout_alignEnd="@+id/broadcast_btn" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/broadcast_preference_btn"
        android:layout_below="@+id/textView"
        android:layout_toStartOf="@+id/broadcast_btn"
        android:layout_alignBottom="@+id/broadcast_btn"
        android:src="@android:drawable/ic_menu_preferences" />

</RelativeLayout>
